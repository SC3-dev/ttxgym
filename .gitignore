# Test-only dependency (jsdom). The site itself ships no JavaScript dependencies.
node_modules/
package-lock.json

# Working notes — kept locally, not published with the site
IMPROVEMENTS.md
SCENARIO-REVIEW.md

# Source catalogue for the technical exercises — not part of the published site
*.odt

# The Updates page is local-only for now: the page, the notes it reads, and its
# tests. tools/build-updates.js stays tracked so it is a one-line change to
# publish them again.
updates.html
lib/updates.json
tests/test-updates.js
